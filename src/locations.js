/* eslint-disable no-unused-vars */
import {getUser} from './redux/users/saveUser'
export const location='http://localhost:5000/api'
export const headers={
    'Content-Type':'application/json'
}
export const authHeader={
    
}